name: 'project_dbt_completo'
version: '1.0.0'
config-version: 2

profile: 'default'

model-paths: ['models']
seed-paths: ['seeds']
test-paths: ['tests']
macro-paths: ['macros']

clean-targets:
  - 'target'
  - 'dbt_packages'
  - 'logs'
